import { ComponentFixture, TestBed } from '@angular/core/testing';
import { IntromeiodimiPage } from './intromeiodimi.page';

describe('IntromeiodimiPage', () => {
  let component: IntromeiodimiPage;
  let fixture: ComponentFixture<IntromeiodimiPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(IntromeiodimiPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
